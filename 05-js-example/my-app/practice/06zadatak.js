/* var words = ["quickest", "brown", "fox", "jumps", "the", "lazi", "dog"];

function getLongestWords(words) {
    var longestWord = words[0];
    for (var i = 1; i < words.length; i++) {
        if (words[i].langth > longestWord.length) {
            longestWord = words[i];
        }
    }
    return longestWord;
}
console.log(getLongestWords(words));
console.log(getLongestWords(["a", "aaa", "aa"]));
console.log(getLongestWords(["a", "b", "c"])); */



